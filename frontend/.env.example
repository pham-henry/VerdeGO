# API Configuration
# Backend API URL - adjust if your backend runs on a different port
# For local development: http://localhost:8080
# For Docker Compose: http://localhost:8080
VITE_API_URL=http://localhost:8080

# Google Maps API (Optional)
# Required for transit recommender map features
# Get your API key from: https://console.cloud.google.com/google/maps-apis
# Note: This is exposed in the client bundle, so restrict API key to specific domains/IPs
VITE_GOOGLE_MAPS_API_KEY=
